<Delete me!>

Header files live here, within a subdirectory titled with the corresponding
library's name.

The only exception are configuration headers (e.g. for version), which are
generated and placed in the PROJECT_BINARY_DIR.